node_modules
coverage
.DS_Store
*.log
.env
.env.local
.vite
.husky/_
.claude/

# Build artifacts inside docs/ — preserved by prepare-pages-dir.mjs:
#   docs/adr/, docs/phase3/, docs/postmortem.md, docs/architecture.md,
#   docs/privacy.md, docs/deploy.md
# Other docs/* files are generated by `npm run build` and are committed
# only on the publish step (so Pages can serve them).
